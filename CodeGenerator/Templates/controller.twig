<?php
declare(strict_types=1);

namespace App\{{ controller_namespace|replace({'\\\\': '\\'}) }};

use Kununu\CQRS\Bundle\Controller\AbstractCommandQueryBusController;

final class {{ class_name }}Controller extends AbstractCommandQueryBusController
{
    public function __invoke(Request $request): JsonResponse {
        return $this->commandBus->handle(new Command($data));
    }
}