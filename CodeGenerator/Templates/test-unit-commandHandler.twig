<?php
declare(strict_types=1);

namespace App\Tests\Unit\UseCase\Command\{{ use_case_name }};

use App\UseCase\Command\{{ class_name }}\Command;
use App\UseCase\Command\{{ class_name }}\CommandHandler;
use PHPUnit\Framework\TestCase;

class CommandHandlerTest extends TestCase
{
    private Command $command;
    private CommandHandler $commandHandler;

    public function testCommandHandler()
    {
        $this->markTestSkipped('This test has not been implemented yet.');
        $this->commandHandler->handle($this->command);
    }

    protected function setUp(): void
    {
        $this->command = new Command();
        $this->commandHandler = new CommandHandler();
    }
}
