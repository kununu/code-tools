{% set query = files.getFileByType("query") %}
{% set queryHandler = files.getFileByType("query-handler") %}
<?php
declare(strict_types=1);

namespace {{ file.namespace }};

use {{ query.fqcn }};
use {{ queryHandler.fqcn }};
use PHPUnit\Framework\TestCase;

class QueryHandlerTest extends TestCase
{
    private Query $query;
    private QueryHandler $queryHandler;

    public function testQueryHandler(): void
    {
        $this->markTestSkipped('This test has not been implemented yet.');
        $this->queryHandler->handle($this->query);
    }

    protected function setUp(): void
    {
        $this->query = new Query();
        $this->queryHandler = new QueryHandler();
    }
}
