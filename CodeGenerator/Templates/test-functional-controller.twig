<?php
declare(strict_types=1);

namespace App\Tests\Functional\{{ controller_namespace|replace({'\\\\': '\\'}) }};

use Kununu\TestingBundle\Test\RequestBuilder;
use Kununu\TestingBundle\Test\WebTestCase;

class {{ class_name }}ControllerTest extends WebTestCase
{
    private const string URI = 'http://example.com';

    public function testReturnsSuccessfulResponse(): void
    {
        $response = $this->doRequest(
            RequestBuilder::aGetRequest()
                ->withUri(self::URI)
                ->withHeader('Content-type', 'application/json')
        );

        self::assertTrue($response->isSuccessful());
    }
}
