name: Qodana
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  qodana:
    name: Qodana
    uses: kununu/shared-github-workflows/.github/workflows/backend_libraries_tests_and_code_quality.yml@main
    secrets: inherit
    with:
      php-version: 8.3
      php-extensions: openssl
