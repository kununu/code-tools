<?php
declare(strict_types=1);

namespace {{ full_namespace }};

{% if request_body and request_body.content['application/json'].schema.properties %}
{% if templates.hasTemplate('command-dto') %}
{% set dto = templates.getTemplateByType('command-dto') %}
use {{ dto.fqcn }};
{% endif %}
{% endif %}
use Kununu\CQRS\Command\Command as CQRSCommand;

final readonly class {{ classname }} implements CQRSCommand
{
    public function __construct(
{% if request_body and request_body.content['application/json'].schema.properties %}
        public {{ entity_name }} ${{ entity_name|lower }},
{% endif %}
    ) {
    }
} 