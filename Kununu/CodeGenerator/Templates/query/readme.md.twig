# {{ operation_id|properCapitalize }}

{{ description|default('This query returns data for ' ~ operation_id|replace({'get': ''})) }}.

{% if parameters is defined and parameters|length > 0 %}
- Query Parameters:
{% for param in parameters %}
{% if param.in == 'path' %}
    - **{{ param.name }}** ([Uuid](https://github.com/kununu/utilities/blob/master/src/DTO/Uuid/Uuid.php))
      - {{ param.description|default('UUID of the ' ~ param.name) }}.
{% endif %}
{% endfor %}
{% if parameters|filter(p => p.in == 'query')|length > 0 %}
    - **[Criteria](./Criteria/Criteria.php)**
      - Criteria to filter the results.
{% for param in parameters %}
{% if param.in == 'query' %}
        - `{{ param.name }}` - `{{ param.schema.type }}` ({{ param.required ? 'required' : 'optional' }} / query param)
          - {{ param.description|default('Filter by ' ~ param.name) }}
{% endif %}
{% endfor %}
{% endif %}
{% endif %}


[Back to Index](../../README.md)